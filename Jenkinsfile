pipeline {
	agent any
	stages {
		stage('Build') {
			steps {
				echo "Pre-Build"
				echo "Build"
			}
		}
		stage('Test') {
			steps {
				echo "Pre-Test"
				echo "Test"
			}
		}

		stage('Integration Test') {
			steps {
				echo "Integration Test"
			}	
		}
	}

	
}
